# Optional drivers for live backends. Install only the ones you need:
#   pip install -r requirements-live.txt
# PostgreSQL live checks
psycopg2-binary==2.9.9
# MySQL / Cloud SQL live checks
PyMySQL==1.1.1
# MongoDB live checks
pymongo==4.7.2
# GCP IAM + Storage live checks
google-cloud-storage==3.10.1
google-cloud-resource-manager==1.12.5
google-auth==2.29.0
